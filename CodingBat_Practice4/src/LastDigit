public boolean lastDigit(int a, int b, int c) {
  int x = a % 10;
  int y = b % 10;
  int z = c % 10;
  int count = 0;
  if (x == y) count++;
  if (y == z) count++;
  if (x == z) count++;
  if (count >= 1) {
    return true;
  }
  return false;
}
